## download Alpine docker image (alpine operating system WITHOUT any kernel BECAUSE containers use the kernel of the HOST machine)
## that is binaries, libraries, etc ...
FROM alpine
### create folder /app inside the container
WORKDIR /app
### copy the source code into the container
### copy 1 file
##COPY src/index.php /app
### copy all the files
COPY src/ /app
### run the following command inside the container: create a webserver running on port 8080
## php -f index.php -S 0.0.0.0:8080
### run the following command
## with brackets process will be executed by the Kernel, without brackets commands will be executed by the shell
## no brackets is equivalent to: sh -c "php -f index.php -S 0.0.0.0:8080" -> PID 1 = sh
## with brackets: php -f index.php -S 0.0.0.0:8080 -> PID 1 = php
ENTRYPOINT ["php"]
### pass these arguments to the command
CMD ["-f", "index.php", "-S", "0.0.0.0:8080"]
